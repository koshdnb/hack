---
# Deployments, Jobs
components:
  client:
    hpa:
      target: 75
      minReplicas: 2
      maxReplicas: 5
    replicas: 2
    resources:
      requests:
        cpu: 1
        memory: "512Mi"
      limits:
        cpu: 1
        memory: "512Mi"
