---
# Deployments, Jobs
components:
  client:
    hpa:
      target: 75
      minReplicas: 1
      maxReplicas: 2
    replicas: 1
    resources:
      requests:
        cpu: 0.01
        memory: "60Mi"
      limits:
        cpu: 0.1
        memory: "60Mi"
